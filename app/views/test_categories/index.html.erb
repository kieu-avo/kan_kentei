<%= content_for(:title, t('.title')) %>
<div class="container pt-32 mx-auto mb-10">
  <h1 class="text-center md:text-5xl text-4xl md:pt-20 pt-14 px-5">今日は、</b>
      <p class="py-5">どこで<span class="bg-amber-100 rounded-full px-3 pb-1">勘</span>をためしますか？</h1>
  <!--検索-->
  <%= render 'search_form' %>
  <!--サンプル問題の続き-->
  <div class="flex justify-center pt-10 px-16 text-center">
    <p class="text-lg md:text-2xl font-bold">サンプル問題(ベトナム級)の続きは<%= link_to "こちらから", category_reviews_path(@vietnam_quiz), class:"underline underline-offset-1 cursor-pointe" %>
  </div>

  <!--写真かgooglemapを表示-->
  <div class="flex justify-center space-x-20 pt-10">
    <%= image_tag 'map3.png', class:"w-96 md:w-full" %>
  </div>

  <!--カテゴリ表示-->
  <div class="md:mr-26 mx-7 flex flex-col md:flex-row justify-center md:space-x-20">
    <% if @categories.any? %>
      <div class="px-12 mx-10 max-w-fit pb-5 rounded-lg bg-neutral-50 bg-opacity-90">
        <p class="pb-5 pt-3 text-xl text-center font-black">🇯🇵 日本編 🇯🇵</p>
        <div class="flex grid grid-cols-2 gap-3 gap-5  whitespace-nowrap px-10 h-fit md:text-xl text-md">
          <%= render 'regions', { categories: @categories, region: 'japan'} %>
        </div>
      </div>
      <div class="pl-5 pr-3 mx-10 md:mt-0 mt-5 max-w-fit pb-5 rounded-lg bg-neutral-50 bg-opacity-90">
        <p class="pb-5 pt-3 text-xl text-center font-black">🌎 海外編 🌍</p>
        <div class="flex grid grid-cols-2 gap-3 gap-4  whitespace-nowrap px-10 h-fit md:text-xl text-md">
          <%= render 'regions', { categories: @categories, region: 'foreign'} %>
        </div>
      </div>
    <% else %>
      <div class="flex items-center space-x-3">
        <%= image_tag 'sorry.png', class: "w-10" %>
        <p class="text-xl">該当の検定はありません。</p>
      </div>
    <% end %>
  </div>

</div>